services:
  - type: web
    name: aye-market-bot
    env: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt && npm install
    startCommand: uvicorn telegram_gateway:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /status
    autoDeploy: true
