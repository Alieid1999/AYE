# Netlify Configuration for AYE Store

# Build settings
[build]
  # Public folder to deploy
  publish = "public"
  
  # No build command needed - static files only
  command = "echo 'Deploying static files from public directory'"
  

# Redirect rules
[[redirects]]
  from = "/*"
  to = "/store_dashboard.html"
  status = 200
